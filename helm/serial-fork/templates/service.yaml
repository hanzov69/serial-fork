apiVersion: v1
kind: Service
metadata:
  name: {{ include "serial-fork.fullname" . }}
  labels:
    {{- include "serial-fork.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "serial-fork.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
